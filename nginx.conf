server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;

    # SPA fallback under /demo/
    location /demo/ {
        try_files $uri $uri/ /demo/index.html;
    }

    # Redirect root to app
    location = / {
        return 301 /demo/;
    }
}
